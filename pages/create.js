import React from 'react';
import { Header, PokemonForm } from '../components';

const create = () => {
    return (
        <div className="px-20">
            <Header />
            <PokemonForm />
        </div>
    );
};

export default create;
